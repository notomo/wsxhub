app:
    restart: always
    build: .
    env_file:
        - .env
    ports:
        - "${HOST_OUTSIDE_PORT}:${CONTAINER_OUTSIDE_PORT}"
        - "${HOST_INSIDE_PORT}:${CONTAINER_INSIDE_PORT}"
    command: "-outside=${CONTAINER_OUTSIDE_PORT} -inside=${CONTAINER_INSIDE_PORT}"
    volumes:
        - ".:/go/src/github.com/notomo/wsxhub"
